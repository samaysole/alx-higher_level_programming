-- a script that creates a table called first_table in the current database
-- and its description is 'id' (INT) and 'name' (VARCHAR(256))
CREATE TABLE IF NOT EXISTS first_table(id INT NOT NULL, name VARCHAR(256) NOT NULL);